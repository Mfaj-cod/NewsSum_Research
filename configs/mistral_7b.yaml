data:
  train_file: data/newssumm_processed/newssumm_processed.json
  max_input_length: 4096
  max_target_length: 256
  val_split: 0.01

experiment:
  name: mistral_7b_run_001
  output_dir: results/mistral_7b_run_001

model:
  name: mistralai/Mistral-7B-Instruct-v0.2

system:
  device: auto

training:
  batch_size: 1
  epochs: 1
  fp16: true
  learning_rate: 1e-5
  seed: 42
